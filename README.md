# MiniDataFrame
MiniDataFrame is a lightweight C++ project that reads CSV files and displays them as formatted tabular data in the console. It provides pandas-like functionality such as column alignment, row and column truncation, and head/tail viewing, while remaining simple and dependency-free.
